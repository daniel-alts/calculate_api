const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const divide = (a, b) => a / b;
const multiply = (a, b) => a * b;
const mean = (q, b) => a + b / 2;

module.exports = {
  add,
  subtract,
  divide,
  multiply,
  mean,
};
